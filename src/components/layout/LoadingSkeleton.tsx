import React from "react";
import Layout from "./Layout";

const LoadingSkeleton = () => {
    return (
        <Layout>
            <h1>Loading</h1>
        </Layout>
    );
};

export default LoadingSkeleton;

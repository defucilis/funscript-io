import React from "react";

import Layout from "../layout/Layout";

const Home = () => {
    return (
        <Layout>
            <h1>Template React App</h1>
            <p>
                This is slightly more useful than the default template, since it has layout and nav
                already built in!
            </p>
        </Layout>
    );
};

export default Home;
